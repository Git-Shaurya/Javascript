/*
4. Declare an array named `citiesVisited` containing
"Mumbai" and "Sydney".
Add "Berlin" to the array using the `push` method.
*/

let citiesVisited = ["Mumbai" , "sydeney"];
citiesVisited.push("berlin");
console.log(citiesVisited);

